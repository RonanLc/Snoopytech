Cours : https://www.mysqltutorial.org
Exo : https://appp.me/z4VN5K

sql : structured query language


* sudo apt install mysql-client-core-8.0 (ou update)
sudo mysql_secure_installation

* wget -c https://repo.mysql.com//mysql-apt-config_0.8.13-1_all.deb

* sudo mysql_secure_installation

* sudo systemctl status mysql (pour verifier si mysql activé)

* sudo mysql -u root -p (pour se connecter au serveur)

* wget https://www.mysqltutorial.org/wp-content/uploads/2018/03/mysqlsampledatabase.zip (telecharge le dossier zip)
* unzip <dossier>

* sudo mysql -u root -p (pour se connecter au serveur)

* mysql> source /home/maximilien/Desktop//ALgo/TD_SQL/mysqlsampledatabase.sql (ajouter database)

* mysql> use classicmodels; (change la database courante pour classicmodels)
* mysql> select * from customers; (query data from customers table) ?

( * mysql> show tables;) (montre les tables de la database courante);

* sudo mysql -u root -p (pour se connecter au serveur)

* mysql> ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'password'; (hange le mot de passe du root pour database)

******* root peut acceder de maniere securiser à la database ********

* mysql> CREATE USER 'username'@'host' IDENTIFIED WITH authentication_plugin BY 'password'; (on crée un utilisateur)

* mysql> GRANT ALTER, INSERT, UPDATE, DELETE, SELECT ON classimodels.* TO 'username'@'host';

* mysql> ALTER USER 'maximilien'@'localhost' IDENTIFIED WITH auth_socket; (pour pouvoir se connecter sans saisir de mot de passe)

********* 

bzip2 -k <file> (pour compresser un fichier sans le supprimer)








